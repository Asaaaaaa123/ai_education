# Clerk — https://dashboard.clerk.com → API Keys (secret key)
CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: restrict which front-end origins are valid for session tokens (azp).
# Comma-separated. If unset, the app allows common local dev URLs.
# CLERK_AUTHORIZED_PARTIES=http://localhost:3333,http://127.0.0.1:3333
