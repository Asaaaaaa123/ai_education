services:
  # Backend Service
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: specialcare-backend
    ports:
      - "8001:8001"
    volumes:
      # Persistent volume for data files (binds to local directory)
      - ./backend/data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
      - DEBUG=False
    command: python start.py
    networks:
      - specialcare-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Frontend Service
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # Frontend calls backend via host port forwarding (localhost:8001)
        # Browser runs on host, not in Docker network, so use localhost
        REACT_APP_API_URL: http://localhost:8001
    container_name: specialcare-frontend
    ports:
      - "3456:3000"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - specialcare-network
    restart: unless-stopped

networks:
  specialcare-network:
    driver: bridge
